package classes

import (
	"backend/models"
	"fmt"
)

func BloodHunterLevelOne(character *models.Character) (*models.Character, error) {
	return nil, fmt.Errorf("couldnt Add Bloodhunter level to the character")

}
